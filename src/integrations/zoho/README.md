# Zoho integration (placeholder — Phase 2)

Not implemented yet. Reserves the extension point for Zoho Social (scheduled
posts and social engagement).

Implement by adding a `connector.ts` implementing `DataConnector`
(`src/integrations/connector.ts`) with `source: 'zoho'`, plus the relevant
migrations, repositories, API routes, and env vars. Keep this a placeholder
until built for real — no stubbed logic.
